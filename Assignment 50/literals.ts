//template literals are something like this '${}'

let morning: string = "early in the morning"

let mustInclude: string = "excersises"

let work: string = "pursuing meaningful work"

let end: string = "spending quality time with loved ones"

const ideal_day: string = `
1)My ideal day starts with waking up ${morning}.
2)It must include all the workout and ${mustInclude}.
3)As time goes by i would be ${work} and it ends with ${end}.`

console.log(ideal_day)